<h2>New Collection</h2>

<ul class="nav nav-tabs">
</ul>

<% if params[:pid].present? %>
  <%= f.hidden_field :parent, :value => params[:pid] %>
<% end %>

<div class="tab-content">
  <div class="tab-pane active" id="local">
    <%= render partial: 'data_form' %>
  </div>

</div>

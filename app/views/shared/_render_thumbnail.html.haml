- if thumbnail.instance_of? ImageThumbnailFile 
	%figure
		%img{src: sufia.download_path(thumbnail.pid, {datastream_id: 'content'}) }
		%figcaption= thumbnail.description
- else
	%div
		%i{ class: thumbnail } 
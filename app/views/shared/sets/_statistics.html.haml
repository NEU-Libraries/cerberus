%div
  .row-fluid
    .span10
      .view_count
        = "Views: Calculating..."
      %br
      .download_count
        = "Downloads: Calculating..."
      %br
      .stream_count
        = "Streams: Calculating..."

:javascript
  $(document).ready(function(){
    $.getScript("#{statistics_count_path(@set.pid)}");
  });

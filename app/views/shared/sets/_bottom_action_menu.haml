- if !current_user.nil? && ( current_user.can? :edit, @set )
  %a(data-add-to-set="1"data-pid="#{@set.pid}" type="button" class="btn btn-clear btn-add" data-placement="left" href="javascript:;")
    %i.icon-plus-sign-alt
    Add

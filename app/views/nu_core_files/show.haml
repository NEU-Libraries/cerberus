- thumbs = @nu_core_file.thumbnail_list
- layout_class =  thumbs.length > 5 ?  'span12' : 'span6'

= render :partial => 'nu_core_files/breadcrumbs'
%article{class: ['drs-item', 'drs-item-full', @nu_core_file.type ],  data: { drsitem: @nu_core_file.pid } }
  %header
    / %h1.page-header= @nu_core_file
    %h1.page-header= kramdown_parse(@nu_core_file.title)

  %section.row
    %div{class: layout_class}
      = render :partial => 'nu_core_files/media_display'
    %div{itemscope: "", itemtype: "http://schema.org/CreativeWork", class:  [ layout_class, 'drs-item-details'] }
      / = render partial: 'show_actions'
      .drs-item-add-details
        / = render partial: 'show_descriptions'
        / = render partial: 'show_details'
      -if @nu_core_file.content_objects
        %section.drs-item-derivatives
          = render partial: 'show_derivatives', locals: {contents: @nu_core_file.content_objects}
      / = render :partial => 'users/activity_log', :locals => {:events => @events}
    / / /.span6
  / / /section.row

- content_for :page_title, @message.subject

-# Same registry shell as the inbox list this was opened from, and as the admin
-# registries: header strip carrying the subject and the one action, bordered
-# card beneath. It previously sat in a flat .well, which read as a different
-# product from the table the reader had just clicked out of.
.admin-registry.my-3.inbox-message
  %header.admin-registry__head
    %h1.admin-registry__title.h6.mb-0
      %i.fa-solid.fa-envelope-open{'aria-hidden' => 'true'}
      = @message.subject
    .admin-registry__head-actions
      = button_to message_path(@message), method: :delete, class: 'btn btn-outline-danger btn-sm',
        form: { data: { turbo_confirm: 'Dismiss this message from your inbox?' } } do
        %i.fa-solid.fa-trash-can.me-1{'aria-hidden' => 'true'}
        Dismiss

  .admin-registry__body
    .inbox-message__meta.text-muted
      From
      = message_sender(@message, @sender_names)
      - if @message.group_addressed?
        to
        = message_group_chip(@message)
      ·
      = message_timestamp(@message.created_at)
    .inbox-message__body= message_body(@message.body)

= link_to messages_path, class: 'btn btn-link ps-0' do
  %i.fa-solid.fa-arrow-left.me-1{'aria-hidden' => 'true'}
  Back to inbox

%ul.breadcrumb
  %li
    = link_to 'Admin Panel', '/admin'
  %li.active
    = link_to 'Manage Core Files', '/admin/files'

%h2.page-header Core Files

%br
%ul.nav.nav-tabs
  %li.active
    = link_to "Tombstoned (#{@count_for_tombstone})", '#tombstoned'
  %li
    = link_to "Incomplete (#{@count_for_incomplete})", '#incomplete'
  %li
    = link_to "In Progress (#{@count_for_in_progress})", '#in_progress'

.tab-content
  #tombstoned.tab-pane.active
    = render partial: "tombstoned", locals: { core_files: @tombstoned }

  #incomplete.tab-pane
    = render partial: "incomplete", locals: { core_files: @incomplete }

  #in_progress.tab-pane
    = render partial: "in_progress", locals: { core_files: @in_progress }


  :javascript
    $('.nav-tabs a').click(function (e) {
      e.preventDefault();
      $(this).tab('show');
    });
    var url = document.location.toString();
    if (url.match('#')) {
        $('.nav-tabs a[href=#'+url.split('#')[1]+']').tab('show') ;
    }

(function($){
  var partial = $('<%= j render( :partial =>  "remote_data_form", :locals => { compilation: @compilation } ) %>');
  partial.find('.modal-footer').prepend(partial.find('input[name="commit"'));
  drsApp.compilationsModal(partial);

}(jQuery));


  

  
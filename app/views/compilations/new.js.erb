(function($){
  var partial = $('<%= j render( :partial =>  "shared/sets/data_form", :locals => { compilation: @compilation, remote: true } ) %>');

  drsApp.compilationsModal(partial, "");

}(jQuery));

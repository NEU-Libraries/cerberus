%p Files associated with this compilation are: 
%ul
  - entries.each do |entry| 
    %li 
      %p= entry.title.first
      = link_to("View #{entry.title.first}", entry)
      %span.spacer //
      = link_to("Remove from compilation", delete_entry_path(id: compilation.pid, entry_id: entry.pid), method: 'delete') 
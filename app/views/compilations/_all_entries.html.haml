%h3
  Files
%ul.media-list
  - entries.each do |entry| 
    %li.media
      %a.pull-left{href: nu_core_file_path(entry)}
        %img.media-object(src="http://placekitten.com/85/85" alt="Kitten Placeholder Image")
      .media
        %h4.media-heading 
          %a{href: nu_core_file_path(entry)}
            = entry.title
        -if entry.description.length > 0
          %p
            =entry.description
        %p
          = link_to("Remove from compilation", delete_entry_path(id: compilation.pid, entry_id: entry.pid), method: 'delete') 

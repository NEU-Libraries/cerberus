<%# Cerberus override of Blacklight's _results_pagination (the below-results
    slot). Renders the same mirrored bar used at the top of the results, so the
    count + full page links bracket the list identically top and bottom. %>
<%= render 'results_pagination_bar' %>

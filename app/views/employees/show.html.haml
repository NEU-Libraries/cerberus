- content_for :page_header do
  %h1.page-header
    = "#{@employee.employee_name}"

= render partial: "show_personal_graph", locals: { :generated_smart_collections => @employee.user_smart_collections, :user_smart_collections => @employee.user_personal_collections, :editable => false }

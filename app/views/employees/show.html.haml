- content_for :page_header do
  %h1.page-header
    = "#{@employee.employee_name}"
    %nav{role: "navigation", class: ["pull-right"]}
      %ul.nav.nav-tabs
        %li
          %a{ href: employee_files_path(@employee) }
            %i.icon-file
            ="#{@employee.employee_name}'s files"

= render partial: "smart_and_user_collections", locals: { :generated_smart_collections => @system_collections, :user_smart_collections => @user_collections, :editable => false }

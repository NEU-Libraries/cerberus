# Use postgres/example user/password credentials
version: '3.7'
services:
  jobs:
    volumes:
      - storage:/home/cerberus/storage
  web:
    build: .
    volumes:
      - ./:/home/cerberus/web
      - storage:/home/cerberus/storage
